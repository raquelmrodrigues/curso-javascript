// escreva uma fução que recebe dois argumentos, largura e altura
// de uma imagem e retorne se a imagem está no modo paisagem

function ePaisagem (largura, altura) {
    return largura > altura;
}

const ePaisagem = (largura, altura) => largura > altura;